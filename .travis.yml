language: node_js
node_js:
  - "stable"
cache:
  directories:
  - node_modules
script:
  - cd react-example
  - npm install
  - npm run build
  - cd ../topup
  - npm install
  - cd ../leverage
  - npm install
  - npm run build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master
